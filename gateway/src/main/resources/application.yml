spring:
  application:
    name: gateway

server:
  port: 8084

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: full

services:
  application:
    url: http://application:8082
  deal:
    url: http://deal:8081

logging:
  level:
    com.example.gateway: DEBUG